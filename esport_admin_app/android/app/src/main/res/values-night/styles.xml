<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night-mode theme for Esport Adda Admin App.
         Used when the device is in Dark Mode (Android 10+).
         MaterialComponents.DayNight is crash-safe on Samsung/Xiaomi/custom ROMs. -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>

    <!-- Theme applied once the Flutter engine has started (V2 embedding only). -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
